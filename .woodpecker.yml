matrix:
  RUBY_VERSION:
    - "3.3"
    - "4.0"

steps:
  test:
    image: ruby:${RUBY_VERSION}
    commands:
      - curl -fsSL https://bun.sh/install | bash
      - export PATH="$HOME/.bun/bin:$PATH"
      - bundle install
      - rake test
